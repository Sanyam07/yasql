tasks:
  - name: simple
    doc: >
      Expected output:
        select col1, col2 from table1 where col1 = 'foo' and col2 = 1
    select:
      fields: [col1, col2]
      from: table1
      where:
        col1: foo
        col2: 1
